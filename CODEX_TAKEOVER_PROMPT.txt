Use $uj-projekt only if a one-time repository/control-layer scaffold is still required, then switch immediately to $credit-efficient-project-runner.

This is a first Codex takeover of an already implemented ChatGPT-built product. The existing `index.html` is the authoritative V038 baseline. Do NOT regenerate it, rewrite it wholesale, split the single-file HTML/CSS/JS structure, or discard any existing work.

Read only: `AGENTS.md`, `STATUS.md`, and once for this takeover `IMPORT_HANDOFF.md`, then inspect the actual artifact and Git state. If the real branch/HEAD/dirty state differs from what is documented, stop and report; do not reset.

Scope now: import/scaffold and verify project state only. No feature work, no UI change, no business-logic change, no release, no push, no tag.

Acceptance:
- baseline artifact preserved byte-for-byte;
- minimal Git/project control layer initialized if needed;
- targeted static checks only (JS parse, duplicate IDs, exactly 2 numeric inputs);
- report baseline hash, files created/changed, Git status, checks PASS/FAIL, and blockers.

Stop after takeover verification. Do not start a next feature.
