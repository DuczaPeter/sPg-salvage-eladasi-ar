# GitHub Pages

The root `index.html` can be deployed directly with GitHub Pages. The app calls external APIs at runtime, so HTTPS deployment is recommended. The standalone single-file user release is also preserved under `release/`.

Keep the fan-site notice visible on every public deployment.
