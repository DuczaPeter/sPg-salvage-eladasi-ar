# GitHub Pages

A repository root `index.html` fájlja közvetlenül használható GitHub Pages kiadásként. Az app runtime külső API-kat ér el, ezért HTTPS-en érdemes publikálni. A single-file user release ettől függetlenül a `release/` mappában is megmarad.

A fan-site notice legyen mindig látható a publikált oldalon.
